# TKMiND backend requirements.
# Mirrors the pinned versions this build was developed and tested against.

# Web framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4

pydantic-settings==2.14.2

# Structured data store (SQLite) + migrations
sqlalchemy==2.0.51
alembic==1.18.5

# Ported PixelRAG ingestion/retrieval pipeline (ai/)
pymupdf==1.28.0          # fitz — PDF to images
faiss-cpu==1.14.3
requests==2.34.2
Pillow==12.3.0
numpy==2.5.1

# GPT-4o vision (design alignment, rerank, answer synthesis)
openai==2.48.0
# Claude text extraction (requirements & change requests)
anthropic==0.123.0
python-docx==1.2.0

# Approved-requirements PDF export
reportlab==5.0.0

# Config/secrets
python-dotenv==1.2.2
PyYAML==6.0.3

# Tests
pytest==9.1.1
pytest-mock==3.15.1
httpx==0.28.1            # required by fastapi.testclient.TestClient
